package com.lenovo.anyshare;

import android.graphics.Paint;

/* loaded from: classes6.dex */
public class JIc extends Paint {
    public JIc() {
        setAntiAlias(true);
        setDither(true);
        setStyle(Paint.Style.STROKE);
        setStrokeJoin(Paint.Join.ROUND);
        setStrokeCap(Paint.Cap.ROUND);
    }
}
